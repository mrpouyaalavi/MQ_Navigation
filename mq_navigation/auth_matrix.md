# Auth Matrix (Removed)

Authentication was removed from the MQ Navigation Flutter app.

The app starts directly at `/home` with no login, signup, MFA, biometric, or profile flows.

The web application retains full authentication support via Supabase Auth. This file is preserved for historical reference of the original auth design.
