// Copy this file to `google_maps_config.js` (same directory) and paste your
// Google Maps JavaScript API key below. The real file is gitignored so your
// key is never committed. When present, `web/index.html` injects the Maps JS
// API script tag and the Google Maps renderer uses real Google tiles.
//
// Without this file the app falls back to an OpenStreetMap renderer (labelled
// in the bottom-left of the Google Maps tab).
//
// Get a key: https://developers.google.com/maps/documentation/javascript/get-api-key
window.GOOGLE_MAPS_API_KEY = "YOUR_GOOGLE_MAPS_JS_API_KEY_HERE";
